export interface TaskCardType {
  title: string;
  description: string;
  status: string;
  author: string;
  createdAt: string;
  createdDate: string;
  updatedAt?: string;
  updatedDate?: string;
}
